import React from "react";
import style from "./square.module.scss";

const Square = () => {
  return (
    <div className={`${style.text_black} ${style.bg_primary_light_3}`}>
      Square asd
    </div>
  );
};

export default Square;
